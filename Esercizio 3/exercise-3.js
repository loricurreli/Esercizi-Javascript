const students = ['Paul', 'George', 'Lucas'];

function addStudent(student) {
  students.push(student); //metodo push aggiunge l'elemento in coda all'array
}

addStudent('Marco');
console.log(students);